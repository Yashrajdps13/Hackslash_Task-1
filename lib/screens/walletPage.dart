import 'package:flutter/material.dart';
class walletPage extends StatelessWidget {
  const walletPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Hello Wallet"),
      ),
    );
  }
}
